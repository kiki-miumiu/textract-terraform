provider_installation {
    direct {
      exclude = ["registry.terraform.io/*/*"]
    }
    network_mirror {
        url = "https:///artifactory/api/terraform/io.terraform.registry/providers/"
    }
}
